import SortPicker from './components/sort-picker';

function install(Vue){
  Vue.component(SortPicker.name, SortPicker);
}
export {
  SortPicker,
  install
};
export default {
  SortPicker,
  install
};

